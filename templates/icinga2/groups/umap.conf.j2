    vars.sslcert["SSLCert umap.openstreetmap.de"] = {
        vars.http_certificate = "20,10"
        vars.http_vhost = "umap.openstreetmap.de"
        groups = [ "certificates" ]
    }

    vars.http["HTTPS umap.openstreetmap.de"] = {
        vars.http_vhost = "umap.openstreetmap.de"
        vars.http_expect = "HTTP/1.1 200 OK"
        vars.http_ssl = true
        vars.http_uri = "/de/"
    }

