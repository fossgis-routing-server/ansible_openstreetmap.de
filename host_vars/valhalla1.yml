# SPDX-License-Identifier: MIT
# Service host: runs valhalla_service (blue/green on 8000+8001),
# nginx (HTTP API + web app), serves valhalla1.openstreetmap.de
# and valhalla.openstreetmap.de.

# Used as a from= restriction in valhalla2's authorized_keys for the
# valhalla-deploy account, so only this host can ssh in to trigger builder
# rebuilds.
valhalla__public_ipv4: 162.55.2.221
